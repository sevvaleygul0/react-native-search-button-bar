export const textInput = (
  height,
  width,
  borderWidth,
  borderColor,
  placeholder,
  placeholderTextColor,
) => ({
  borderRadius: 24,
  padding: 10,
  width,
  height,
  borderWidth,
  borderColor,
  placeholder,
  placeholderTextColor,
});
